/* Time constants. The demo dataset was removed in Phase 2 — all data now comes
   from the control-plane API (see lib/api.ts + lib/data.tsx). */
export const MIN = 60_000;
export const HR = 60 * MIN;
export const DAY = 24 * HR;
