# Ansible inventory — the hosts Rudder targets. Rudder parses this to populate
# the Inventory screen. (Connection auth is supplied from Vault at run time.)
[targets]
target

[targets:vars]
ansible_user=rudder
